<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="8dp"
    android:id="@+id/poster_container"
    android:layout_width="128dp"
    android:layout_height="172dp"
    app:layout_constraintTop_toTopOf="parent"
    app:cardCornerRadius="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/item_cast_poster"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <TextView
            android:id="@+id/cast_member"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:textColor="#FFFFFF"
            android:layout_marginStart="6dp"
            app:layout_constraintBottom_toTopOf="@id/character"
            app:layout_constraintStart_toStartOf="@id/item_cast_poster" />

        <TextView
            android:id="@+id/character"
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:textColor="#FFFFFF"
            android:textStyle="bold"
            android:layout_margin="6dp"
            android:textSize="14sp"
            app:layout_constraintBottom_toBottomOf="@id/item_cast_poster"
            app:layout_constraintStart_toStartOf="@id/item_cast_poster" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>